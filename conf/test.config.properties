#################################
# Codeshelf Server Configuration
#################################

# Web App internal server configuration
webapp.hostname=localhost
webapp.portnum=8443

# Uncompiled source - access with URL = https://localhost:8443/src/html/codeshelf.dev.html to access the uncompiled version 
# webapp.content.path=../CodeshelfUX

# Compiled source
webapp.content.path=../CodeshelfUX/target/web

# Websocket listener configuration
websocket.hostname=localhost
websocket.portnum=8444

# Database connection 
db.name=database
db.schemaname=codeshelf
db.userid=codeshelf
db.password=codeshelf
db.address=localhost
db.portnum=5432

# Facility configuration - will be removed after supporting tenant configuration
facility.sequencer=BayDistance
#facility.sequencer=BayDistanceTopLast

#################################
# Site Controller Configuration
#################################

# disable radio for integration tests
radio.enabled=false

# Server to connect to
websocket.uri=wss://127.0.0.1:8444

# Login information
organizationId=TestOrg
facilityId=F1
networkId=DEFAULT
networkCredential=0.6910096026612129

# Radio settings
codeshelf.networknum=1
codeshelf.preferred.channel=5

pcapbuffer.enable=true
pcapbuffer.size=65536
pcapbuffer.slack=4096
